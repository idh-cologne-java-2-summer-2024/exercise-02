package idh.java;

public class userAccount {
	/* class in which userAccounts as objects with variables are defined and with a constructor to assign values to these variables. AserAccounts need
	 * an accountNumber, user Balance and a userName.
	 */
 int accountNumber;
 int userBalance; 
 String userName; 
 
 public userAccount(int number, int balance, String name) {
	 this.accountNumber=number; 
	 this.userBalance=balance; 
	 this.userName=name;
	 
 }
 
 public static void main(String[] args) {
		
		
	}
}

